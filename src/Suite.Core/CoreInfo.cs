namespace WindowsCareKit.Core;

/// <summary>Placeholder marker so the project compiles during scaffolding; real contracts land next.</summary>
public static class CoreInfo
{
    public const string ProductName = "Windows Care Kit";
}
